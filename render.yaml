services:
  - type: web
    name: skan-backend
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn app.main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: DATABASE_URL
        value: sqlite:///./skan.db
      - key: JWT_SECRET
        generateValue: true
      - key: EMAIL_USER
        value: skan.egypt@gmail.com
      - key: EMAIL_PASS
        value: iqza zlwz phom knsq
      - key: BASE_URL
        value: https://skan-xi.vercel.app
      - key: PYTHON_VERSION
        value: 3.11.0